linters-settings:
  custom:
    loglint:
      type: module
      description: Linter for log messages rules
      original-url: github.com/Nishiramirai/loglint

linters:
  disable-all: true
  enable:
    - loglint
